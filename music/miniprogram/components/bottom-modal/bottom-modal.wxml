<view class="modal" hidden="{{!modalShow}}">
  <view class="panel">
    <i class="iconfont icon-guanbi_huaban1" bind:tap="closeModal"></i>

    <!-- 具名插槽 -->
    <slot name="modal-content"></slot>
  </view>
</view>